{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "if [ -f demo-page/package.json ]; then cd demo-page && npm install; else npm install; fi"
  },
  "deploy": {
    "startCommand": "if [ -f demo-page/package.json ]; then cd demo-page && npm start; else npm start; fi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
